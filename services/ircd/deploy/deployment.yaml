apiVersion: apps/v1
kind: Deployment
metadata:
  name: ircd
  labels:
    app: ircd
spec:
  replicas: 1 # Will increase in prod
  selector:
    matchLabels:
      app: ircd
  template:
    metadata:
      labels:
        app: ircd
    spec:
      containers:
      - name: ircd
        # The tag will be dynamically substituted by GitHub Actions using sed before apply
        image: ghcr.io/eran-meir/irc/ircd:305cd217e94efc6a48a5d495dcc8b2196e796956
        imagePullPolicy: Always
        ports:
        - containerPort: 6667
        env:
        - name: VALKEY_URL
          value: "valkey:6379"
